{
  "name": "brams",
  "version": "0.4.0",
  "private": true,
  "description": "Dependency-free industrial UI component catalog",
  "scripts": {
    "preview": "python3 -m http.server 8080",
    "test": "playwright test",
    "test:chromium": "playwright test --project=chromium",
    "test:update": "playwright test --project=chromium --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
